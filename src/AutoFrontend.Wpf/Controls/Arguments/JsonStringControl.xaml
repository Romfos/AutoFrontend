<UserControl xmlns:Design="clr-namespace:AutoFrontend.Wpf.Controls.Design"  x:Class="AutoFrontend.Wpf.Controls.Arguments.JsonStringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Margin="0,5,0,5"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label x:Name="label" Margin="0" Padding="0"/>
        <TextBox x:Name="textBox"
             VerticalScrollBarVisibility="Disabled" 
             HorizontalScrollBarVisibility="Auto"
             AcceptsReturn="True" 
             AcceptsTab="True" 
             MinHeight="50">
            <TextBox.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="generateRandomValue" Header="Generate random value"/>
                </ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>
        <Design:ValidationControl x:Name="validationControl"/>
    </StackPanel>
</UserControl>
