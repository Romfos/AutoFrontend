<UserControl xmlns:Design="clr-namespace:AutoFrontend.Wpf.Controls.Design"  
             x:Class="AutoFrontend.Wpf.Controls.Functions.QueryFunctionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Margin="0,5,0,5">
    <GroupBox x:Name="groupBox">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <TextBlock TextAlignment="Center">Auto refresh</TextBlock>
                </CheckBox>
                <Button x:Name="executeButton" 
                    Content="Refresh"
                    Padding="5"
                    Grid.Column="3"/>
            </Grid>
            <ProgressBar x:Name="progressBar" IsIndeterminate="True" Visibility="Collapsed" Height="10" />
            <StackPanel x:Name="resultStack" />
            <Design:ExceptionControl x:Name="exceptionControl"/>
        </StackPanel>
    </GroupBox>
</UserControl>
